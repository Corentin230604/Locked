<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>LockedAgent</AssemblyName>
    <RootNamespace>LockedAgent</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Socket.IO v4-compatible client used to talk to the Node backend in real time. -->
    <PackageReference Include="SocketIOClient" Version="3.1.2" />
    <!-- Graphics.CopyFromScreen for periodic screenshot capture. -->
    <PackageReference Include="System.Drawing.Common" Version="8.0.8" />
  </ItemGroup>

</Project>
