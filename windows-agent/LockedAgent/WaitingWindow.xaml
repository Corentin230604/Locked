<Window x:Class="LockedAgent.WaitingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Locked" Height="280" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Topmost="True"
        Background="{StaticResource BackgroundBrush}"
        FontFamily="Segoe UI">
    <Grid Margin="28">
        <Border Background="{StaticResource CardBrush}" CornerRadius="16"
                Padding="24" BorderBrush="#EAEAEC" BorderThickness="1">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="En attente du démarrage" FontSize="20" FontWeight="Bold"
                           Foreground="{StaticResource TextBrush}" HorizontalAlignment="Center"
                           TextWrapping="Wrap" TextAlignment="Center"/>
                <TextBlock x:Name="ExamTitleText" FontSize="13"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" Margin="0,6,0,0" TextWrapping="Wrap"
                           TextAlignment="Center"/>
                <TextBlock Text="Excel est ouvert en arrière-plan. L'examen démarrera automatiquement dès que l'intervenant le lancera."
                           FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" Margin="0,18,0,0" TextWrapping="Wrap"
                           TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
